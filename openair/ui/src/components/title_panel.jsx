import React from 'react';

let OptionsTitle = props => (
    // props: {title}
    <div className="w3-border-bottom w3-border-top default-primary-color">
        {props.title}
    </div>
);

export default OptionsTitle



